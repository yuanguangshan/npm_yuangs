SPECIFICATION Spec

CONSTANTS
  Actions = {act1, act2}
  Agents = {user_admin, ai_agent}
  Rights = {read, write, execute}
  CapabilityTokens = {cap1, cap2}
  States = {"DRAFT", "PROPOSED", "APPROVED", "EXECUTED", "OBSERVED", "VERIFIED", "REJECTED"}

INVARIANTS
  TypeOk
  ExecutionRequiresApproval
  CapabilityRequired
  NoExtraChanges
  NoExecuteAfterRevocation
  NoDoubleExecute
